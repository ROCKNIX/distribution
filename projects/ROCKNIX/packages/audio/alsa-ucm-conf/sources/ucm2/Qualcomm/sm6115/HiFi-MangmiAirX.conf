# Use case configuration for Mangmi Air X (SM6115).
# Audio path: Q6 AFE -> Secondary MI2S -> AW88261 speakers
# PCM mapping: hw:0,0 = Playback (MM1)
# ES8326 (headphones + mic) is disabled in DT while bring-up continues.
# Volume is handled by PipeWire soft-mixer (api.alsa.soft-mixer = true).

SectionVerb {

	EnableSequence [
		cset "name='SEC_MI2S_RX Audio Mixer MultiMedia1' 1"
	]

	DisableSequence [
		cset "name='SEC_MI2S_RX Audio Mixer MultiMedia1' 0"
	]

	Value {
		TQ "HiFi"
	}
}

SectionDevice."Speaker" {
	Comment "Speaker Playback"

	EnableSequence [
		cset "name='SPK_L PCM Playback Volume' 720"
		cset "name='SPK_R PCM Playback Volume' 720"
	]

	DisableSequence [
		cset "name='SPK_L PCM Playback Volume' 0"
		cset "name='SPK_R PCM Playback Volume' 0"
	]

	Value {
		PlaybackPriority 100
		PlaybackPCM "hw:${CardId},0"
		PlaybackChannels 2
		PlaybackRate 48000
	}
}
