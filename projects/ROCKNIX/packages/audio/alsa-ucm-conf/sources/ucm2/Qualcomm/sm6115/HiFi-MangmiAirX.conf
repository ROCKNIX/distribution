# Use case configuration for Mangmi Air X (SM6115).
# Audio paths:
#   Speaker:    MM1 -> SEC_MI2S_RX        -> AW88261 (playback PCM 0)
#   Headphones: MM2 -> QUAT_MI2S_RX       -> ES8326  (playback PCM 1)
#   Mic:        MM4 <- TX_CODEC_DMA_TX_3  <- bolero TX macro DEC2 <- DMIC2
#                                            (LPI gpio8 clk, gpio9 data)

SectionVerb {

	EnableSequence [
		cset "name='SEC_MI2S_RX Audio Mixer MultiMedia1' 1"
		cset "name='QUAT_MI2S_RX Audio Mixer MultiMedia2' 1"
	]

	DisableSequence [
		cset "name='SEC_MI2S_RX Audio Mixer MultiMedia1' 0"
		cset "name='QUAT_MI2S_RX Audio Mixer MultiMedia2' 0"
	]

	Value {
		TQ "HiFi"
	}
}

SectionDevice."Speaker" {
	Comment "Speaker Playback"

	ConflictingDevice [
		"Headphones"
	]

	EnableSequence [
		cset "name='SPK_L PCM Playback Volume' 360"
		cset "name='SPK_R PCM Playback Volume' 360"
	]

	DisableSequence [
		cset "name='SPK_L PCM Playback Volume' 0"
		cset "name='SPK_R PCM Playback Volume' 0"
	]

	Value {
		PlaybackPriority 100
		PlaybackPCM "hw:${CardId},0"
		PlaybackChannels 2
		PlaybackRate 48000
		PlaybackMixer "default:${CardId}"
	}
}

SectionDevice."Headphones" {
	Comment "Headphone Playback (ES8326)"

	ConflictingDevice [
		"Speaker"
	]

	EnableSequence [
		cset "name='Headphones Playback Volume' 120"
	]

	DisableSequence [
		cset "name='Headphones Playback Volume' 0"
	]

	Value {
		PlaybackPriority 200
		PlaybackPCM "hw:${CardId},1"
		PlaybackChannels 2
		PlaybackRate 48000
		PlaybackVolume "Headphones Playback Volume"
		JackControl "Headphone Jack"
		JackHWMute "Speaker"
	}
}

SectionDevice."Mic" {
	Comment "Built-in microphone (DMIC2 via bolero TX macro)"

	EnableSequence [
		cset "name='MultiMedia4 Mixer TX_CODEC_DMA_TX_3' 1"
		cset "name='TX DMIC MUX2' DMIC2"
		cset "name='TX_AIF1_CAP Mixer DEC2' 1"
		cset "name='TX_DEC2 Volume' 102"
	]

	DisableSequence [
		cset "name='TX_AIF1_CAP Mixer DEC2' 0"
		cset "name='MultiMedia4 Mixer TX_CODEC_DMA_TX_3' 0"
	]

	Value {
		CapturePriority 100
		CapturePCM "hw:${CardId},5"
		CaptureChannels 1
		CaptureRate 48000
	}
}
