#!/bin/sh
# Enable charging on SGM41515 charger IC
# The BQ256XX driver may not set CHG_CONFIG during hw_init due to
# probe ordering with the FUSB302 TCPM and OTG regulator.
# REG01 bit 4 = CHG_CONFIG: 1 = charging enabled

i2cset -f -y 1 0x1a 0x01 0x1a 2>/dev/null
