[Unit]
Description=ROCKNIX input-sense service
After=headphones.service
Before=rocknix.target
StartLimitIntervalSec=0

[Service]
Environment=HOME=/storage
ExecStart=/bin/bash -c "source /etc/profile && exec /usr/bin/input-sense"
Restart=always
RestartSec=2

[Install]
WantedBy=multi-user.target
