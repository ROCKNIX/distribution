Dolphin's Steam Deck backend opens the controller's hidraw, which makes
hid-steam drop its gamepad; the core gets its input from RetroArch.

--- a/Source/Core/InputCommon/ControllerInterface/ControllerInterface.h
+++ b/Source/Core/InputCommon/ControllerInterface/ControllerInterface.h
@@ -35,7 +35,7 @@
 #if defined(HAVE_SDL3)
 #define CIFACE_USE_SDL
 #endif
-#if defined(HAVE_HIDAPI)
+#if defined(HAVE_HIDAPI) && !defined(__LIBRETRO__)
 #define CIFACE_USE_STEAMDECK
 #endif
 
