diff --git a/parallel-rdp/interface.cpp b/parallel-rdp/interface.cpp
index c94550f..83023eb 100644
--- a/parallel-rdp/interface.cpp
+++ b/parallel-rdp/interface.cpp
@@ -100,6 +100,10 @@ static size_t fragment_size;
 std::vector<bool> rdram_dirty;
 uint64_t sync_signal;
 
+static bool guide_held = false;
+static bool r2_held = false;
+static bool fast_forward_toggled = false;
+
 static TTF_Font *message_font;
 static std::queue<Message> messages;
 static uint64_t message_timer;
@@ -212,7 +216,117 @@ bool sdl_event_filter(void *userdata, SDL_Event *event) {
       break;
     }
   }
+else if (event->type == SDL_EVENT_GAMEPAD_BUTTON_DOWN)
+{
+    switch (event->gbutton.button)
+    {
+    case SDL_GAMEPAD_BUTTON_GUIDE:
+        guide_held = true;
+        break;
+
+    case SDL_GAMEPAD_BUTTON_RIGHT_SHOULDER: // R1
+        if (guide_held)
+        {
+            callback.save_state = true;
+            return 1;
+        }
+        break;
+
+    case SDL_GAMEPAD_BUTTON_LEFT_SHOULDER: // L1
+        if (guide_held)
+        {
+            callback.load_state = true;
+            return 1;
+        }
+        break;
+
+    case SDL_GAMEPAD_BUTTON_WEST: // West
+        if (guide_held)
+        {
+            display_fps = !display_fps;
+            rdp_onscreen_message(
+                display_fps ? "FPS display ON" : "FPS display OFF",
+                false
+            );
+            return 1;
+        }
+        break;
+
+    case SDL_GAMEPAD_BUTTON_SOUTH: // South
+        if (guide_held)
+        {
+            callback.reset_game = true;
+            rdp_onscreen_message("Reset game", false);
+            return 1;
+        }
+        break;
+
+    case SDL_GAMEPAD_BUTTON_START: //Start
+        if (guide_held)
+        {
+            if (gfx_info.fullscreen)
+                callback.emu_running = false;
+            return 1;
+        }
+        break;
+
+    case SDL_GAMEPAD_BUTTON_MISC1: // Share
+        if (guide_held)
+        {
+          SDL_RunOnMainThread(ra_display_inprogress_achievements, nullptr, true);
+        }
+        break;
 
+    default:
+        if (guide_held)
+            return 1; // block other inputs while guide held
+        break;
+    }
+}
+else if (event->type == SDL_EVENT_GAMEPAD_AXIS_MOTION)
+{
+    switch (event->gaxis.axis)
+    {
+    case SDL_GAMEPAD_AXIS_RIGHT_TRIGGER: // R2
+    {
+        bool pressed = event->gaxis.value > 0;
+
+        if (pressed && !r2_held && guide_held)
+        {
+            fast_forward_toggled = !fast_forward_toggled;
+            callback.enable_speedlimiter = !fast_forward_toggled;
+
+            rdp_onscreen_message(
+                fast_forward_toggled ? "Fast forward on"
+                                     : "Fast forward off",
+                false
+            );
+
+            r2_held = true;
+            return 1;
+        }
+
+        if (!pressed)
+            r2_held = false;
+    }
+    break;
+
+    default:
+        break;
+    }
+}
+else if (event->type == SDL_EVENT_GAMEPAD_BUTTON_UP)
+{
+    switch (event->gbutton.button)
+    {
+    case SDL_GAMEPAD_BUTTON_GUIDE:
+        guide_held = false;
+        break;
+
+    default:
+        break;
+    }
+}
   return 0;
 }
 
@@ -317,9 +431,9 @@ void rdp_init(void *_window, GFX_INFO _gfx_info, const void *font,
   }
 
   message_font =
-      TTF_OpenFontIO(SDL_IOFromConstMem(font, font_size), true, 25.0);
+      TTF_OpenFontIO(SDL_IOFromConstMem(font, font_size), true, 30.0);
   achievement_challenge_indicator_font =
-      TTF_OpenFontIO(SDL_IOFromConstMem(font, font_size), true, 12.0);
+      TTF_OpenFontIO(SDL_IOFromConstMem(font, font_size), true, 30.0);
   if (!message_font || !achievement_challenge_indicator_font) {
     rdp_close();
     return;
diff --git a/src/ui/input.rs b/src/ui/input.rs
index af59d65..6b7798e 100644
--- a/src/ui/input.rs
+++ b/src/ui/input.rs
@@ -418,6 +418,13 @@ pub fn get(ui: &ui::Ui, channel: usize) -> InputData {
                 .offset(i32::from(sdl3_sys::scancode::SDL_SCANCODE_RALT) as isize)
     };
 
+    let guide_pressed = !controller.is_null() && unsafe {
+        sdl3_sys::gamepad::SDL_GetGamepadButton(
+            controller,
+            sdl3_sys::gamepad::SDL_GamepadButton::GUIDE,
+        )
+    };
+
     for i in 0..14 {
         if profile_name != "default" || channel == 0 {
             let profile_key = profile.keys[i];
@@ -428,7 +435,7 @@ pub fn get(ui: &ui::Ui, channel: usize) -> InputData {
             }
         }
 
-        if !controller.is_null() {
+        if !controller.is_null() && !guide_pressed {
             set_buttons_from_controller(profile, i, controller, &mut keys);
         } else if !joystick.is_null() {
             set_buttons_from_joystick(profile, i, joystick, &mut keys);
@@ -442,7 +449,7 @@ pub fn get(ui: &ui::Ui, channel: usize) -> InputData {
         (x, y) = set_axis_from_keys(profile, ui.input.keyboard_state);
     }
 
-    if !controller.is_null() {
+    if !controller.is_null() && !guide_pressed {
         (x, y) = set_axis_from_controller(profile, controller);
         apply_deadzone(&mut x, &mut y, profile.deadzone);
     } else if !joystick.is_null() {
