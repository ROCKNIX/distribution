# Load rtl8733bu-power so rfkill can cut WiFi/BT chip power when WiFi is off.
# Must load at boot (systemd-modules-load) so the platform driver probes before
# the quirk runs; modprobe in the quirk can run too late or fail silently.
rtl8733bu_power
