--- a/drivers/power/supply/bq256xx_charger.c	2026-03-25 00:39:47
+++ b/drivers/power/supply/bq256xx_charger.c	2026-03-26 01:43:54
@@ -1563,6 +1563,11 @@
 	int wd_reg_val = BQ256XX_WATCHDOG_DIS;
 	int ret = 0;
 	int i;
+
+	/* Reset chip to clear stale register values (e.g. from Android) */
+	regmap_update_bits(bq->regmap, BQ256XX_PART_INFORMATION,
+			   BQ256XX_REG_RST, BQ256XX_REG_RST);
+	msleep(10);
 
 	for (i = 0; i < BQ256XX_NUM_WD_VAL; i++) {
 		if (bq->watchdog_timer == bq256xx_watchdog_time[i]) {
@@ -1586,19 +1591,7 @@
 
 	if (ret) {
 		dev_warn(bq->dev, "battery info missing, default values will be applied\n");
-
-		bat_info->constant_charge_current_max_ua =
-				bq->chip_info->bq256xx_def_ichg;
-
-		bat_info->constant_charge_voltage_max_uv =
-				bq->chip_info->bq256xx_def_vbatreg;
-
-		bat_info->precharge_current_ua =
-				bq->chip_info->bq256xx_def_iprechg;
 
-		bat_info->charge_term_current_ua =
-				bq->chip_info->bq256xx_def_iterm;
-
 		bq->init_data.ichg_max =
 				bq->chip_info->bq256xx_max_ichg;
 
@@ -1626,7 +1619,8 @@
 		return ret;
 
 	ret = bq->chip_info->bq256xx_set_iprechg(bq,
-				bat_info->precharge_current_ua);
+				bat_info ? bat_info->precharge_current_ua :
+				bq->chip_info->bq256xx_def_iprechg);
 	if (ret)
 		return ret;
 
@@ -1636,7 +1630,8 @@
 		return ret;
 
 	ret = bq->chip_info->bq256xx_set_iterm(bq,
-				bat_info->charge_term_current_ua);
+				bat_info ? bat_info->charge_term_current_ua :
+				bq->chip_info->bq256xx_def_iterm);
 	if (ret)
 		return ret;
 
@@ -1646,7 +1641,8 @@
 			return ret;
 	}
 
-	power_supply_put_battery_info(bq->charger, bat_info);
+	if (bat_info)
+		power_supply_put_battery_info(bq->charger, bat_info);
 
 	return 0;
 }
